# Shared dev dependencies — pinned (see ai-backend/requirements.txt for the
# service deps and the bump procedure).
ruff==0.15.21
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1

# Service-specific requirements: ai-backend/requirements.txt
